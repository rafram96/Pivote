# Dependencias del backend on-prem (validador + scrapers + scripts).
# El lado cliente (skill) es Node — sus dependencias van en skill/package.json.

# Contrato y validación
pydantic>=2.0

# Excel final enriquecido
openpyxl>=3.1

# Scraping SUNAT / InfoObras (backend/scraping/)
requests>=2.31
rapidfuzz>=3.0          # fuzzy match razón social (sunat.py)

# Persistencia: hoy en ARCHIVOS (RepositorioArchivos). Cuando se cablee
# PostgreSQL (repo + caché SUNAT) se re-agrega psycopg2-binary.

# API HTTP (backend/api/)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0      # carga .env en api/app.py (config de entorno)

# Dev (tests offline en backend/tests/ — no se necesita en el servidor)
pytest>=8.0
httpx>=0.27          # TestClient de FastAPI
