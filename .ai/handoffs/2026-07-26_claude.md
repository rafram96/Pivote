# Handoff 2026-07-26 — preparación de 3 carpetas de fixtures para probar el backend nuevo

**Pedido:** preparar `fixtures/ing manuel/Pronis_Sonitor`, `Consorcio-Lircay` y `Soritor2`
para probar el backend nuevo — la duda concreta era *"no sé si estoy repitiendo documentos"*.

## Respuesta a la duda

**No hay propuestas repetidas.** Lo único duplicado son **las bases** del concurso Soritor,
triplicadas (una copia por carpeta de postor): ≈ **273 MB recuperables**. Los nombres se parecen
porque los **postores** se llaman parecido, no porque los archivos se repitan.

Índice nuevo: [`fixtures/ing manuel/_CONCURSOS.md`](../../fixtures/ing%20manuel/_CONCURSOS.md).

## Identidades resueltas (leídas del Anexo N° 1 de cada propuesta)

**Concurso CP N° 01-2026-PRONIS-1 · EESS Soritor · CUI 2540065** — 3 postores, 1 carpeta cada uno:

| Carpeta | Postor | Técnica |
|---|---|---|
| `Soritor/` | CONSORCIO SALUD SORITOR | 434 p · escaneo |
| `Pronis_Sonitor/` | CONSORCIO SUPERVISOR EESS SORITOR | 597 p · 347 MB · escaneo |
| `Soritor2/` | CONSORCIO SUPERVISOR SORITOR | 218 p · **born-digital** |

⚠ "SUPERVISOR EESS SORITOR" ≠ "SUPERVISOR SORITOR": distintos RUC y distinto representante común.

**Concurso CP N° 02-2025/GOB.REG.HVCA/C · Hospital II-1 Lircay · CUI 2326667** — `Consorcio-Lircay/`
tiene **2 postores × 2 tomos** (no 1 postor en 4 archivos): CONSORCIO SUPERVISIÓN HOSPITAL LIRCAY
(718 folios) y CONSORCIO HOSPITALARIO LIRCAY / "motlima dial" (952 folios). Es el **concurso Lircay
original** del pivote → hay golden (los 3 Excels de Manuel) para contrastar la corrida.

## Qué se preparó

`_prep/MAPA.md` en las 3 carpetas, con el formato de `HuachoColpa/_prep/MAPA.md`: inventario
(páginas/tamaño/capa de texto), offsets folio↔página verificados, tachado de las bases, descartes
razonados y pendientes antes de correr. Más:

- `Pronis_Sonitor/_prep/subpdfs/` — la técnica de 347 MB troceada en **6 sub-PDFs, todos < 100 MB**
  (el `Read` no abre el original). Folio = página 1:1, verificado 597/597.
- `Soritor2/` y `Consorcio-Lircay/` — **no requieren troceo**, todas las piezas ya entran.
- `bases_texto.txt` + `bases_texto.md` (texto sin tachado + reporte) en las 3.

**Se descartó fusionar los tomos/partes**: contradice el runbook
(`docs/preparacion-pdfs-corrida.md` §4, el `Read` falla > 100 MB). Los unidos que se habían
generado (349 / 140 / 156 MB) se borraron.

## Hallazgos que cambian veredictos

1. **Soritor (las 3 carpetas)** — el factor **C. SOSTENIBILIDAD SOCIAL** está **tachado =
   eliminado** (págs 94 y 96, 1655 chars). No debe puntuar. Confirma de forma determinística el
   flag que en `Soritor/_PREPARACION.md` estaba anotado a ojo.
2. **Lircay** — las bases se llaman *"sin tachas"* pero **tienen tachado en 16 páginas**
   (2482 chars). Lo eliminado toca **equipamiento estratégico** (pick-up 2021+, `(INCL. SOFTWARE)`,
   `(INCL. MIRAS)`, `(Con Estación Total)`) y **todas las funciones BIM del personal** (BEP, LOD,
   4D/5D, IFC/RVT/NWC). Exigir eso sería exigir requisitos derogados.
3. **Lircay — falta la oferta económica** de ambos postores → sin límite inferior (bases: 90% de la
   cuantía) ni puntaje de precio. **Pedírselas a Manuel.**
4. **Lircay — doble numeración.** Cada tomo trae un contador digital que **reinicia** (texto
   extraíble, 1..N) y encima el sello de folio del expediente (imagen, corrido). En el postor A el
   folio del Tomo II = página + 336 (verificado: 00336 → 00337 → 00718). En el postor B el sello del
   Tomo II está superpuesto con otro y **queda por confirmar**. Los subagentes deben citar
   **tomo + página local**.

## Herramientas nuevas (reutilizables, en `src/tools/`, untracked)

| Script | Qué hace |
|---|---|
| `detectar_tachado_pdf.py` | Detecta el tachado de un PDF born-digital por geometría de la línea, char a char, y emite texto limpio + reporte por página. Para cuando **solo hay PDF** (con DOCX va `skill/scripts/limpiar_bases_docx.py`). El equivalente usado en HuachoColpa fue ad-hoc y no se había guardado. |
| `trocear_pdf.py` | Trocea un PDF en sub-PDFs < 100 MB sin re-rasterizar (preserva el folio); subdivide solo si una parte se pasa. |

**Validación:** `detectar_tachado_pdf.py` reprodujo exactamente las 10 páginas con tachado del
reporte ya auditado de HuachoColpa (19, 20, 22, 23, 63, 65, 66, 67, 68, 72).

## Siguiente paso

- **Probar el backend nuevo con `Soritor2/`** — es el fixture más liviano (95 MB, born-digital, sin
  troceo, con índice de secciones extraíble por texto).
- Pre-render a PNG donde haga falta (Camino B) — no se hizo: el pedido era preparar, no correr.
- Decidir si se borran las copias duplicadas de bases del concurso Soritor (≈273 MB).
- Los 2 scripts de `src/tools/` están **sin commitear**.
