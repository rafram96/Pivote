# Handoff 2026-07-28 — el día de la identidad (auditoría Lircay → 9 fixes) y la pérdida de la golden

> Rama de trabajo: **`fix/identidad-lircay`** (12+ commits sobre `demo`, SIN push).
> Suite al cierre: **637 passed, 24 skipped**. Corrida viva de divino_nino EN CURSO
> (lanzada por Rafael, con Tesseract local — Camino A activo por primera vez).

## 1 · Auditoría del ing. (WhatsApp) → diagnóstico con certificados físicos

Job identificado: **`371fa5a1e704`** (Lircay, postor A "bisbal", P2 = Minchola).
Se extrajeron las págs. 213-216 del Tomo II con pdfimages y se leyeron como imagen:

- **E4 "se inventó obras"**: la skill corrió los folios (E1/E2 llevaban los de
  E3/E4) y la herencia DEDUP transplantó la obra de Picota a Lurigancho → #58.
- **9:3 "baja otro CUI"**: el cert cita 2198316 con **TYPO de 1 dígito** (la
  verdad es **2198318**, C.S. Yanahuanca PASCO ACTIVO; 2198316 = veredas de
  Lambayeque en el MEF) y el paso 0 lo aceptó `PROBABLE` con nombre+dpto+rubro
  contradichos → #59.
- **2:3 "correcto a medias"**: cert físico = SUPERVISIÓN DE LA OBRA; `ruc_match`
  eligió el registro del **EXPEDIENTE TÉCNICO** (36493) → #61.
- El Excel rotulaba el CUI resuelto como "(del certificado)" — probado que los
  certs NO citan CUI → #60.
- Los "días imposibles" del resumen (391>177) eran **código viejo del server**
  (su Excel es pre-#51); en `demo` actual salen coherentes. Fix = redeploy.

## 2 · Implementado en la rama (todo con tests)

| Commits | Qué |
|---|---|
| `936e7c0` `5810d38` `ee17b7c` `02b5cca` `c164d30` | #58 (DEDUP exige emisor) · #59 (tope a la exención del CUI citado) · #61 (veto de fase ET) · #60 (procedencia del CUI en el Excel) |
| `15d2170` | #52 completada: parser de prosa REESCRITO conservador (el previo capturaba **90.0** del "90% de la cuantía" como límite — calibrado contra la prosa real), bloque de oferta SIEMPRE visible + avisos, candado `OFERTA_INCOMPLETA`. #53: prompt mapa multi-tomo (guardrail backend ya estaba) |
| `7b13ead` | #47: verificación folio↔emisor EN LA SKILL + `folio_verificado` en las 3 copias del contrato (zod/Pydantic/consolidador) + Excel NO embebe con `false` |
| `050677c` | Reconstrucción: `replay_enriquecimiento.py` + `exportar_verdades_panel.py` |
| (28-jul) | `test_contrato.py` descubre espejos (rutas stale eliminadas) — CONTRATO OK en 6 espejos |

Todos los cambios del resolver son de **ABSTENCIÓN PURA** (resuelto→revisión).
Replay sobre 152 experiencias reales: los ÚNICOS cambios efectivos son los 3
casos del ing. (el resto de deltas del replay = ruido de snapshot documentado).

## 3 · ⚠ LA GOLDEN SE PERDIÓ (irreversible, confirmado por Rafael)

`auditoria_cui_v3.xlsx` (~277 verdades humanas) + caché + baseline: NO existen
en ninguna máquina. Consecuencias y sustituto en `handoffs/current.md` (nota) y
memoria del proyecto. Regla operativa: **solo cambios de abstención pura** en el
resolver hasta que exista la **golden v4** (cosechar con
`scripts/exportar_verdades_panel.py` EN EL SERVER — cada revisión resuelta en el
panel es una verdad humana). Semilla local: `datos_pivote/auditoria_cui_v4.xlsx`
(5 filas verificadas con certs físicos + base MEF).

## 4 · ETL / issues (mañana temprano)

"Documento primero": épica #12 reducida de 14 issues a 5 ejecutables; el detalle
vive en `docs/backend/modulo_etl_mef.md` (§8 = las 95 columnas REALES de los 3
CSV). **BUG confirmado #19**: el catálogo pierde **12,102 entidades**
(`NOMBRE_*` vs `NOM_*`) — afecta el gate público/privado; es lo más urgente del
ETL y no depende de nada. RAM no es problema (decisión); PG confirmado para el
cold path (#18, recrear cuando ADR-009 cierre); módulo COTIZABLE aparte.

## 5 · Pendientes al cierre (en orden)

1. **Corrida divino_nino** (en curso): al terminar, verificar `folio_verificado`
   en las experiencias, montos del postor, oferta en celdas y
   `funciones_similares` (#31). Es la prueba viva del tren completo.
2. Push + **PR de `fix/identidad-lircay` → `demo`** (cierra #47 #52 #53 #58-61).
3. **Redeploy del server** + regenerar el Excel de Lircay para el ing.
4. Cosechar verdades v4 en el server (`exportar_verdades_panel.py`).
5. #19 (catálogo de entidades) — fix de una tupla, medir efecto en el gate.
6. Del 27 hacia abajo: congeladas por decisión de Rafael (28-jul).
